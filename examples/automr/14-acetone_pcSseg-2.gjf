%mem=32GB
%nprocshared=16
#p CASSCF/pcSseg-2

mokit{ist=3,readrhf='acetone_pcSseg-2_rhf.fch',NMR}

# This example shows how to perform NMR calculations at CASSCF/pcSseg-2 level.
# Since Gaussian does not have built-in pcSseg-2 basis set, we calculate and
# provide one .fch file.

# To run this example, you need to install Gaussian, GAMESS, PySCF and Dalton.
